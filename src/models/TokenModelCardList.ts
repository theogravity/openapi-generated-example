/* istanbul ignore file */
/* tslint:disable */
/* eslint-disable */

import type { TokenModelCard } from './TokenModelCard';

export type TokenModelCardList = {
    total: number;
    page: number;
    perPage: number;
    records: Array<TokenModelCard>;
};
