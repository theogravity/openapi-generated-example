/* istanbul ignore file */
/* tslint:disable */
/* eslint-disable */

import type { PrototypeCard } from './PrototypeCard';

export type PrototypeCardList = {
    total: number;
    page: number;
    perPage: number;
    records: Array<PrototypeCard>;
};
